{
  "name": "hrms",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "supabase": "supabase",
    "supabase:db-reset": "supabase db reset",
    "supabase:types": "supabase gen types typescript --local > src/database.d.ts",
    "test": "bun test",
    "make:build": "make build",
    "make:dev": "make dev",
    "make:protobuild": "make protobuild",
    "make:test": "make test",
    "make:test.nonverbose": "make test.nonverbose"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "bun-types": "latest",
    "supabase": "^1.136.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
