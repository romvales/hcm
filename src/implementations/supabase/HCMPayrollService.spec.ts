import { afterEach, test } from 'bun:test'

let userId: string

afterEach(async () => {
  await cleanHCMPayrollServiceTest()
})

const setupHCMPayrollServiceTest = async () => {

  return {}
}

const cleanHCMPayrollServiceTest = async () => {

}

test('> HCMPayrollService.createPayroll()', async () => {
  const {} = await setupHCMPayrollServiceTest()
})

test('> HCMPayrollService.getPayrollById()', async () => {
  const {} = await setupHCMPayrollServiceTest()
})

test('> HCMPayrollService.deletePayrollById()', async () => {
  const {} = await setupHCMPayrollServiceTest()
})

test('> HCMPayrollService.savePayroll()', async () => {
  const {} = await setupHCMPayrollServiceTest()
})

