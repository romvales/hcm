import { afterEach, test } from 'bun:test'

afterEach(async () => {
  await cleanHCMRoleServiceTest()
})

const setupHCMRoleServiceTest = async () => {

  return {}
}

const cleanHCMRoleServiceTest = async () => {
  
}

test('> HCMRoleService.createRole()', async () => {
  const {} = await setupHCMRoleServiceTest()
})

test('> HCMRoleService.getRoleById()', async () => {
  const {} = await setupHCMRoleServiceTest()
})

test('> HCMRoleService.deleteRoleById()', async () => {
  const {} = await setupHCMRoleServiceTest()
})

test('> HCMRoleService.saveRole()', async () => {
  const {} = await setupHCMRoleServiceTest()
})

